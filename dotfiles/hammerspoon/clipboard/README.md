# Dotori Clipboard

The clipboard history UI is a prewarmed Hammerspoon `hs.webview`. Edit
`web/index.html`, `web/style.css`, or `web/main.js` directly; Hammerspoon
watches and reloads them automatically. Lua inlines the three files at load
time, so no package manager or build step is required.
